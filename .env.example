# Root .env — used by docker-compose.yml for the optional PostgreSQL service.
# Copy to .env (gitignored) before running `docker compose up -d`.
# These must match the credentials in server/.env DATABASE_URL.
POSTGRES_USER=people
POSTGRES_PASSWORD=change-me-in-dev
POSTGRES_DB=people_management
